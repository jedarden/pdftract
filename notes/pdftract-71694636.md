# pdftract-71694636 — standalone sdk-conformance-verify smoke: full disposition (three attempts)

Umbrella bead: **pdftract-71694636** (child of parent pdftract-854dc50c). This note records the
outcome of all three attempts to satisfy the bead's acceptance criteria: the first standalone submission
(consolidated 2026-09-21T05:20Z), the re-run chain (resolved 2026-09-21T07:06Z), and the
harvest → directive → execution split (resolved 2026-09-21T11:25Z). The authoritative
copy of all three records lives in the bead's `notes` field; this file mirrors and extends them.

## Attempt 1 — first submission, never reconciled (consolidated 2026-09-21T05:20Z)

- Workflow `sdk-conformance-verify-manual-s7mbl` (ns argo-workflows, iad-ci), created 2026-09-21T04:05:26Z
  via the sanctioned iad-ci kubeconfig path with the 4 overridden parameters
  (`runtime-image ruby:3.2-slim`, `sdk-repo https://github.com/jedarden/pdftract-ruby.git`,
  `sdk-ref main`, `conformance-command "ruby -v && echo CONFORMANCE-SMOKE-PASS"`).
- **Never reconciled**: metadata frozen at generation 1 with no status subresource for ~72 min;
  no pod ever created. Root cause: iad-ci controller/API-server starvation — the workflow-collection
  LIST the controller depends on exceeded its request timeout, so workflows created in the window
  02:58:32Z–04:34:30Z (47/229 namespace objects, many unrelated templates) were orphaned.
- Consolidation child pdftract-2a31501b closed on this record; static-audit child pdftract-38a015ba
  had already exculpated the manifest (commit a0d99241, later b382676e): live object == HEAD,
  required parameters default-free, no continueOn/retryStrategy, PVC 75Gi sata-large.
- The full verbatim record is in the bead `notes` field (see below for how to read it).

## Attempt 2 — re-run chain (pdftract-1d88c7c0 → pdftract-1511c956 → pdftract-173ba586)

**Disposition: IRREDUCIBLE-FAIL (incident-bound; argo-workflows-budget ResourceQuota starvation).**
No re-run workflow exists — the chain deliberately made zero cluster writes.

| Chain child | Verdict | Key fact |
|---|---|---|
| pdftract-1d88c7c0 (health gate) | STILL-STARVED (~06:23Z) | Controller/workqueue plane recovered, but every collection LIST still EOF/timeout'd and quota saturated (cpu 3500m/3500m, mem 5Gi/5Gi). Directive: do not submit; harvest the live first run instead. |
| pdftract-1511c956 (resubmit + capture) | INCIDENT-BOUND FAIL | No submission. Harvests 06:32:28Z / 06:39:50Z: s7mbl Running, clone-sdk PENDING quota-forbidden, object frozen between reads. |
| pdftract-173ba586 (outcome resolution) | IRREDUCIBLE-FAIL | Live re-check 06:44:52Z (generation 10). Manifest exculpated again against the LIVE object (byte-identical to declarative-config HEAD 4225e1df). Failure is ResourceQuota admission — none of the in-scope plumbing modes. |

## Fresh evidence at rollup (2026-09-21T07:04:44Z)

Single-object raw GET via `kubectl --server=http://traefik-iad-ci:8001` (succeeded on first attempt):

- `sdk-conformance-verify-manual-s7mbl`: `phase=Running`, `startedAt=2026-09-21T05:57:42Z`,
  `finishedAt=null`, `generation=11`, `rv=85370071` — the orphan WAS picked up after the
  controller recovered and is alive, but stuck at its first step.
- Node `clone-sdk` (type Pod) still PENDING, message verbatim:

  ```
  pods "sdk-conformance-verify-manual-s7mbl-clone-sdk-4074569654" is forbidden: exceeded quota: argo-workflows-budget, requested: requests.cpu=300m,requests.memory=384Mi, used: requests.cpu=3400m,requests.memory=5Gi, limited: requests.cpu=3500m,requests.memory=5Gi
  ```

  Blocked on both dimensions (cpu 100m free vs 300m requested; memory 0 free vs 384Mi requested).
  This admission-rejection message is the only step-output text that exists anywhere in the chain —
  `run-conformance` never started, so its echo output does not exist.

## Open tail (not resolved by the rollup)

`activeDeadlineSeconds 7200` from `startedAt 05:57:42Z` means s7mbl reaches a terminal
Failed/deadline phase on its own at ~07:57:42Z if quota never frees. If quota frees first, it
resumes at clone-sdk and the conformance command's exit code (`ruby -v && echo CONFORMANCE-SMOKE-PASS`)
decides parent criteria 1–2. Either way: harvest by single-GET poll — never stack a second workflow
on the same quota.

## Acceptance-criteria disposition (parent pdftract-71694636)

1. Standalone shape submitted (bare Workflow + `workflowTemplateRef sdk-conformance-verify` +
   argument overrides, sanctioned kubeconfig path): **MET** in the narrow plumbing sense.
2. Reaches phase Succeeded with the verdict decided by the conformance command's exit code:
   **NOT MET** — no terminal phase has existed at any point through 2026-09-21T07:04:44Z; the
   command never executed.
3. Declarative-config fix required: **NO** — verified twice against the live object; the condition
   is cluster-side (quota/infrastructure) and owned by cluster ops.

## How to re-verify the bead-side records

`bead show` does not display close reasons and the notes field is long; read it directly:

```bash
bead show pdftract-71694636 --json | jq -r '.[0].notes'
```

Rollup child: pdftract-bb368450 (2026-09-21).

---

## Attempt 3 — harvest → directive → execution split, resolved 2026-09-21T11:25Z (child pdftract-8d1e18fb)

**Final disposition: IRREDUCIBLE-FAIL on execution evidence — the conformance command never executed
in ANY run.** This section supersedes the acceptance disposition of Attempt 2 above; the epilogue
below resolves that attempt's open tail (s7mbl reached terminal Failed by deadline at 07:59:34Z, so
quota never freed in time to resume it).

### The split (all three children closed)

| Child | Verdict | Key fact |
|---|---|---|
| pdftract-65163b39 (harvest) | s7mbl terminal Failed 07:59:34Z | Deadline backstop (~112s late, after `startedAt` 05:57:42Z) killed the workflow after clone-sdk spent its whole life quota-starved (pod never created); run-conformance never executed; no verdict. Object frozen 07:04→07:59Z despite quota headroom — informer LIST failures 07:31–07:39Z never re-enqueued it. Read-only. |
| pdftract-bacb99a9 (directive) | GO — exactly one resubmission | All three gates verified live 08:17–08:32Z: terminal-without-verdict; quota headroom (used 4/66 pods, 300m/3500m cpu, 640Mi/5Gi mem); reflector clean (newest error 08:06:50.290528Z, streak >25 min). Fixed parameters, single-GET polling, one-shot limit, explicit blindness condition → record and stop. |
| pdftract-c63e0a93 (execution) | GO executed → STOP on blindness | Workflow `sdk-conformance-verify-manual-656vw` (uid 1a364528-5b29-4ac3-b422-0268c51fd922) created 08:52:55Z after the pre-submit re-check passed 08:50:55Z. The controller never saw it — the 09:06:52Z postpone sweep enumerated 35 informer-store workflows without it; Pending, nodeCount 0, rv frozen at the creation-time 85421877 through 10:16Z; blindness stop 09:19Z, zero retries. Finding: the GO health check (quota + clean logs) is necessary but NOT sufficient — an informer/watch can be silently dead since initial sync with zero error signatures. |

### Epilogue observed fresh at consolidation (single-GETs + controller log pulls 10:15–11:11Z)

- The informer **self-healed without any restart** (same container instance `58864cbbc9-c95zk`,
  started 08:05:25Z, never restarted): the workflow_worker processed 656vw at 11:04:37Z still
  carrying the creation-time `rv=85421877` — zero object mutations in 2h11m.
- 11:04:48Z: phase `""` → `Running` (its first-ever status write); workspace PVC created;
  clone-sdk pod creation rejected by memory quota again (requested 384Mi vs used 5Gi/5Gi; 4992Mi
  at 11:07:29Z) — transient-error retry loop through the last captured line 11:07:52Z
  (capture ends 11:11:06Z).
- **Deleted between 11:11:06Z and 11:16:34Z**: workflow and workspace PVC both NotFound after;
  no GC/TTL/deletion line for it in the captured logs, so the deleting actor is unproven (operator
  cleanup is plausible — the execution child had flagged it for operator attention). No terminal
  phase was ever observed on it; run-conformance never started.
- Quota at consolidation: used 4 pods, 300m/3500m cpu, 640Mi/5Gi mem — ample, and for the third
  time irrelevant. No smoke object remains in the namespace (s7mbl reaped ~08:17Z).

### Final per-criterion disposition (parent pdftract-71694636)

1. Standalone shape submittable via `workflowTemplateRef`: **MET** — both submissions (s7mbl
   04:05:26Z, 656vw 08:52:55Z) accepted by the API server with the 4 parameters verbatim; template
   live and unchanged (gen=2, rv=85080853 at both directive and consolidation time), byte-identical
   to declarative-config HEAD 4225e1df.
2. Reaches Succeeded, verdict from the command's exit code: **NOT MET — FINAL**. run-conformance
   never started in any run; no exit code exists. Run 1: clone-sdk never admitted (quota),
   terminal Failed by deadline backstop 07:59:34Z. Run 2: no pod ever created (informer-invisible,
   then quota-forbidden), object deleted mid-Running. Cluster-side sequence: starvation orphaning →
   quota saturation → silent informer blindness → quota re-saturation. The one-shot resubmission
   budget is spent; the verdict is UNANSWERED by execution — not passed, and not failed on the merits.
3. Declarative-config fix required: **NO** — manifest exculpated three times; every failure was
   cluster-side infrastructure owned by cluster ops. Residual operator concerns: workflow-controller
   reliability, quota volatility, ~35 perpetually-postponed stale workflows under the namespace
   concurrency limit.

Child pdftract-8d1e18fb is the parent's LAST open blocker (siblings 65163b39, bacb99a9, c63e0a93
and earlier 2a31501b, 38a015ba, bb368450 all closed); this child does not close the parent. Once it
closes, the parent's own close decision belongs to the umbrella's dispatch.

Evidence preserved: `/var/tmp/s7mbl-harvest/` (harvest child), `/var/tmp/smoke-c63e0a93/`
(execution child), `/var/tmp/rollup-8d1e18fb/` (this child: controller log pulls covering
10:15:25Z–11:11:06Z and captured reads).
