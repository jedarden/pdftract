# bf-4s11m4: Implement Document type assertion

## Summary
Added a dedicated test function to verify Document type assertion using fixture data, establishing the foundation for nested type checks.

## Work Completed

### File Changes
- **test_sdk_types_smoke.py**: Added `test_type_assertions_from_fixture_data()` function
  - Function loads fixture data from `tests/fixtures/remote_100page.pdf`
  - Calls `pdftract.extract()` with the loaded fixture
  - Adds `isinstance()` assertion for Document type
  - Uses exact error message format: `Expected Document type, got {type(obj)}`
  - Added function to main test runner

### Implementation Details

The new test function `test_type_assertions_from_fixture_data()`:
1. Loads PDF fixture from `tests/fixtures/remote_100page.pdf`
2. Calls `pdftract.extract(pdf_path)` to get the Document object
3. Performs `isinstance(doc, pdftract.Document)` assertion
4. Uses descriptive error message: `f'Expected Document type, got {type(doc)}'`

### Acceptance Criteria Status
- ✅ Test calls the function with fixture data
- ✅ isinstance() assertion for Document type exists
- ✅ Assertion has descriptive error message
- ✅ Test would fail if Document type is incorrect
- ✅ Fix commit: $(git log -1 --format=%H)

## Verification

### Test Execution
```bash
python3 test_sdk_types_smoke.py
```

All tests passed successfully:
- ✅ Document type assertion from fixture data test passed
- ✅ All existing tests continue to pass

### Syntax Validation
```bash
python3 -m py_compile test_sdk_types_smoke.py
# ✓ File is syntactically valid Python
```

### Test Output
```
Testing Document type assertion from fixture data...
✓ Document type assertion passed
✅ Document type assertion test passed!
```

## Notes
- Document is the top-level object type in the SDK
- This test establishes the foundation for nested type checks (Page, Span, Block, Metadata)
- The test follows the pattern of calling the function with fixture data and asserting the return type
- The error message format matches the specification: `Expected Document type, got {type(obj)}`
