# bf-6506g — Fix missing dependencies in fuzz/Cargo.toml

**Type:** implement · **Verified against current source:** 2026-07-22

## Outcome: no missing dependencies remain — already correct

Investigation shows `fuzz/Cargo.toml`'s dependency declarations are **complete and correct**.
Dependency resolution passes fully; there are no "crate not found" or "unknown dependency"
errors. The acceptance criteria were already satisfied by prior beads (notably
`deps(bf-3mull)` which added `serde_yaml`, and `fix(bf-1c4f6)` which fixed the content
harness). No Cargo.toml change was required.

## Evidence

### 1. Current declarations
```toml
[dependencies]
pdftract-core = { path = "../crates/pdftract-core",
                  features = ["profiles", "serde", "decrypt", "quick-xml", "cjk"] }
libfuzzer-sys = { version = "0.4", features = ["arbitrary-derive"] }
serde_yaml = { version = "0.9" }
```

### 2. Every requested feature exists in `pdftract-core` `[features]`
`crates/pdftract-core/Cargo.toml`: `profiles` (`dep:serde_yaml`), `serde`, `decrypt`,
`quick-xml`, `cjk` are all defined. `profile_yaml.rs` calls
`pdftract_core::profiles::load_profile_yaml`, which requires the `profiles` feature — enabled. ✔

### 3. Resolution proceeds past dependency resolution (AC-3)
`cargo tree -e no-dev` from `fuzz/` resolves the full graph with no errors:
```
├── pdftract-core v0.1.0 (...)  cjk,decrypt,default,profiles,quick-xml,serde
├── libfuzzer-sys v0.4.13
└── serde_yaml v0.9.34+deprecated
```
`cargo check` compiles ~15 dependency crates including `pdftract-core` **before** any error. ✔

### 4. The only build blocker is environmental, not a dependency declaration
`cargo check` fails at `libfuzzer-sys`'s native build script:
`ToolNotFound: failed to find tool "c++"`. No C++ compiler (`c++`/`g++`/`clang++`/`cc`) is
installed in this environment. This is downstream of dependency resolution and cannot be fixed
in `Cargo.toml` — it requires a C++ toolchain for cargo-fuzz to build libFuzzer. Out of scope
for this bead (dependency declarations).

## Acceptance criteria status
- [x] All missing deps identified in build errors added — **none were missing.**
- [x] Versions/features match fuzz-target requirements — verified against each target's API use.
- [x] `cargo build` from `fuzz/` proceeds past dependency resolution — **confirmed** (fails only
      later at libFuzzer's C++ build script for lack of a compiler).
- [x] No "crate not found" / "unknown dependency" errors remain — **confirmed** via `cargo tree`.

## Note
`serde_yaml` is declared as a direct fuzz dependency but is used only transitively by
`pdftract-core` (behind `profiles`); no fuzz target uses it directly. It is harmless (an unused
direct dep is a lint at most, never a resolution error) and was left in place — removing it is
out of scope and adds no value to the acceptance criteria.
