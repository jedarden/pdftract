# Bead bf-5239x7 Verification Note

## Task: Call sdk_search with parsed parameters

### Implementation Summary

The `search()` function in `/home/coding/pdftract/crates/pdftract-py/src/lib.rs` (lines 246-298) has been correctly implemented to call `sdk_search` with all required parameters.

### Acceptance Criteria Verification

All acceptance criteria have been met:

| Criterion | Status | Location | Evidence |
|-----------|--------|----------|----------|
| search() signature has `path` (not `_path`) | ✅ PASS | Line 249 | `path: &str` |
| `Path::new(path)` is called to create `pdf_path` | ✅ PASS | Line 270 | `let pdf_path = Path::new(path);` |
| `sdk_search` is called with all 5 parameters | ✅ PASS | Line 271 | `sdk_search(pdf_path, pattern, case_insensitive, use_regex, whole_word)` |
| Error case returns `Err(map_error_to_py(py, err))` | ✅ PASS | Lines 274-279 | `Err(err) => return Err(map_error_to_py(py, err))` |
| Ok case binds to `matches` variable | ✅ PASS | Line 275 | `Ok(matches) => matches` |
| `cargo check --package pdftract-py` passes | ✅ PASS | Compilation | No errors or warnings |

### Implementation Details

The search function:
1. Parses optional kwargs for `case_insensitive`, `regex`, and `whole_word` parameters
2. Creates a `Path` from the input string: `let pdf_path = Path::new(path);`
3. Calls the SDK search function with all five parameters: `sdk_search(pdf_path, pattern, case_insensitive, use_regex, whole_word)`
4. Properly maps Rust errors to Python exceptions using `map_error_to_py`
5. Binds the successful result to the `matches` variable
6. Converts the matches to a Python dictionary with pattern and matches list

### Test Results

- **Compilation**: ✅ `cargo check --package pdftract-py` passes with no errors
- **Function Signature**: ✅ Correctly uses `path: &str` (not underscore-prefixed)
- **Path Creation**: ✅ Correctly calls `Path::new(path)` 
- **SDK Call**: ✅ All five parameters passed to `sdk_search`
- **Error Handling**: ✅ Proper error mapping with `map_error_to_py`
- **Result Binding**: ✅ Ok case bound to `matches` variable

### Conclusion

The implementation is complete and correct. All acceptance criteria pass without any WARN or FAIL items. The bead can be closed.
