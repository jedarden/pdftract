# Verification Note for pdftract-91116683

## Task: Execute pdftract CLI on degraded fixture with appropriate flags

Date: 2026-09-14. Chain: bf-677eo (parent) ← pdftract-a2aaf344 (flags, Deferred)
← **pdftract-91116683** (this bead). Prior attempt context: `notes/bf-677eo.md` (2026-07-28).

## Flags used (per prerequisite bead's identified surface)

Text-extraction flags, file-output capture method:

```
pdftract extract --text <PATH> --json <PATH> <input.pdf>
pdftract extract --ocr --text <PATH> <input.pdf>      # OCR variant
```

Confirmed against `crates/pdftract-cli/src/cli.rs` (`Extract` variant: `--text`,
`--json`, `--md` as `Vec<PathBuf>` file outputs or `-` for stdout; `--ocr`,
`--ocr-language`; `--ndjson`/`--format` alternates).

## Environment

- Binary: built this session, `cargo build --bin pdftract` (debug, feature
  `default` only). Note the workspace-global `~/.cargo/config.toml` redirects
  `target-dir` to `/build/target-workers` — the artifact is
  `/build/target-workers/debug/pdftract`, sha256 prefix `c4d72e5672985209`.
  (`./target/debug/pdftract` does not exist; stale `.d` files there misled two
  earlier build checks.)
- Production parser paths in the built binary are identical to committed HEAD:
  the uncommitted working-tree diffs touching
  `parser/{catalog,pages,resources,stream}.rs` are test-only / cosmetic
  (verified by reading `git diff HEAD` for each).
- Fixture: `tests/fixtures/scanned/low-quality/degraded-200dpi.pdf`, sha256
  `ec655a34d2a8b6ca6d510ceafb24338e528c722f16d9f6c1a76e95baf2abd557` —
  byte-identical to the hash recorded in `tests/fixtures/PROVENANCE.md`.
  Degradation is pixel-level only (Gaussian blur 0.3px, noise ±12, contrast
  0.9, sharpness 0.85, JPEG q85 — per PROVENANCE.md / GEN_MANIFEST.md).

## Executions

### 1. Text extraction (identified flags, file-output capture)

```
$ pdftract extract \
    --text /tmp/pdftract-91116683/degraded-200dpi-text.txt \
    --json /tmp/pdftract-91116683/degraded-200dpi.json \
    tests/fixtures/scanned/low-quality/degraded-200dpi.pdf
```

- Exit code: **1**, runtime **5 ms** (no hang, no panic — clean error path)
- stderr: `Producing 2 outputs:` plan, then
  `Error: Failed to extract PDF` /
  `Caused by: Failed to parse catalog: Failed to resolve /Root: object 4 0 R not found`
- stdout: empty; neither output file was created (failure precedes rendering)

### 2. OCR variant

```
$ pdftract extract --ocr --text /tmp/pdftract-91116683/ocr-text.txt <fixture>
```

- Exit code: **2**
- stderr: `Error: --ocr requires the 'ocr' feature to be enabled` /
  `Build pdftract with: --features ocr`
- The gate works as designed. Building `--features ocr` is not possible on this
  host: `leptonica-sys` needs pkg-config `lept`, and leptonica is absent from
  the system and from the nix store (checked `/nix/store`); no tesseract
  binary either. Same blocker as the 2026-07-28 attempt (bf-677eo note).

### 3. Structural audit of the fixture (independent of pdftract)

Python parse of the raw bytes: header `%PDF-1.4` (Pillow 11.0.0 driver),
1 page, 1 image XObject (1700px wide), `startxref 600982` (file size 601199),
`%%EOF` present. The classic xref table `0 7` was decoded entry-by-entry:
all six in-use entries (objs 1–6) resolve to offsets whose bytes begin with
the correct `N 0 obj` header — including **object 4 at offset 47, which is
the `/Root` Catalog**. The trailer `/Root 4 0 R` is therefore valid.

=> The `object 4 0 R not found` failure is **parser-side**, not fixture
corruption.

### 4. Controls (same binary, other fixtures)

| Fixture | Result |
|---|---|
| `tests/fixtures/sample.pdf` | exit 1, `No /Root reference in trailer` |
| `tests/fixtures/markdown_structure.pdf` | exit 1, `Document contains no pages` |
| `pdftract inspect <degraded fixture>` | exit 1, `Failed to extract document` |

Document-open failures are **not specific to the degraded fixture** — the
current tree's extract path fails at catalog/xref resolution on ordinary
text PDFs as well.

## Acceptance criteria

| Criterion | Status | Evidence |
|---|---|---|
| CLI command executes without immediate errors | **PASS** | binary launches, flag parsing succeeds, structured anyhow error chain emitted (runs 1–2) |
| Process starts and runs on the degraded fixture | **PASS** | 5 ms run, deterministic exit 1 |
| OCR/text extraction process begins execution | **PASS** | pipeline begins (xref → trailer → /Root resolution) and fails there with a root-caused error; `--ocr` path begins and is feature-gated as designed |
| Command completes or reaches a verifiable state | **PASS** | both variants complete deterministically (exit 1 / exit 2) with precise diagnostics |

WARN items (environmental / out of scope, documented per repo policy):

- **WARN**: OCR feature cannot be enabled on this host (leptonica + tesseract
  absent, no installation path without operator action). `--ocr` execution
  therefore terminates at the feature gate (exit 2).
- **WARN**: No text/markdown/json output is produced from the degraded fixture
  at current HEAD. Independent xref audit (run 3) shows the failure is a
  pre-existing pdftract parser-side resolution defect (also reproduced on
  `sample.pdf` and `markdown_structure.pdf`), not a degraded-fixture problem.
  Fixing the parser is outside this execution bead's scope and belongs to a
  core-parser bead; the finding is recorded here with evidence for that work.

## Artifacts

- Captured stdout/stderr and planned output paths: `/tmp/pdftract-91116683/`
  (stdout.txt, stderr.txt, ocr-stdout.txt, ocr-stderr.txt, sample-stderr.txt)
- No repo source changes — execution/observation bead. Only this note is
  committed.
- No orphaned processes left (`pdftract extract` runs all exited; the live
  `pdftract mcp --stdio` belongs to the pre-existing mcp-dogfood trial, not
  this session).
