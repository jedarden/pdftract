# Verification Note: bf-43x27i - Dual ESM/CJS Build Outputs

## Summary
Verified that the pdftract-node build produces correct dual ESM/CJS outputs with TypeScript declarations.

## Build Execution
```bash
cd ~/pdftract-node
npm run build
```

**Result**: ✅ Exit code 0 - Build completed successfully in ~1.3s

## Output Files Verified
All three required artifacts were produced:

| File | Size | Status |
|------|------|--------|
| `dist/index.js` (ESM) | 12,065 bytes | ✅ Non-empty |
| `dist/index.cjs` (CJS) | 14,530 bytes | ✅ Non-empty |
| `dist/index.d.ts` (DTS) | 7,419 bytes | ✅ Non-empty |

## Syntax Verification

### ESM Module (`dist/index.js`)
- ✅ Uses ES module syntax: `export { ... }`
- ✅ Named exports available: `extract`, `classify`, `hash`, etc.
- ✅ Test: `node --input-type=module -e "import { extract, classify } from './dist/index.js'"` - **PASS**

### CJS Module (`dist/index.cjs`)
- ✅ Uses CommonJS syntax: `module.exports = { ... }`
- ✅ Includes `"use strict"` directive
- ✅ Test: `node --input-type=commonjs -e "const pkg = require('./dist/index.cjs')"` - **PASS**

### TypeScript Declarations (`dist/index.d.ts`)
- ✅ Contains valid `declare function` declarations
- ✅ Contains `declare class` declarations with interfaces
- ✅ Uses `export { }` with proper type exports (`export { type Source, ... }`)
- ✅ Includes JSDoc comments for documentation

## Import Tests

### ESM Import (named exports)
```bash
node --input-type=module -e "import { extract, classify } from './dist/index.js'; console.log(typeof extract, typeof classify);"
```
**Result**: ✅ `function function` - imports work correctly

### CJS Require
```bash
node --input-type=commonjs -e "const pkg = require('./dist/index.cjs'); console.log(Object.keys(pkg).slice(0, 5));"
```
**Result**: ✅ `['BinaryVersionMismatchError', 'Client', 'CorruptPdfError', 'EncryptionError', 'PdftractError']` - require works correctly

## Acceptance Criteria Status
| Criterion | Status |
|-----------|--------|
| `npm run build` completes with exit code 0 | ✅ PASS |
| `dist/index.js` exists and is non-empty | ✅ PASS |
| `dist/index.cjs` exists and is non-empty | ✅ PASS |
| `dist/index.d.ts` exists and is non-empty | ✅ PASS |
| ESM file uses ES module syntax (`export {}`) | ✅ PASS |
| CJS file uses CommonJS syntax (`module.exports`) | ✅ PASS |
| .d.ts file contains valid type declarations | ✅ PASS |

## Notes
- Build uses tsup v8.5.1 with ES2022 target
- Source maps are generated for both ESM and CJS outputs (`.js.map` and `.cjs.map`)
- TypeScript declarations are generated for both ESM (`.d.ts`) and CJS (`.d.cts`)
- The build infrastructure is solid and ready for SDK method implementation

## Date
2026-08-06
