# Bead bf-1ct908: Smoke Test for classify_page

## Summary

Successfully implemented a basic smoke test for the `classify_page` function in `/home/coding/pdftract/crates/pdftract-core/src/classify.rs`.

## Implementation

### Test Location
- **File**: `crates/pdftract-core/src/classify.rs`
- **Test Function**: `test_smoke_classify_basic_vector_page`
- **Line**: Added in the test module around line 2323

### Test Coverage

The smoke test verifies:

1. ✅ **Function returns successfully for valid input**
   - Creates a valid `PageContext` with typical vector page metrics
   - Calls `classify_page()` and confirms it returns a `PageClassification`

2. ✅ **Output format is correct**
   - Verifies the result contains `class` field (PageClass enum)
   - Verifies the result contains `confidence` field (f32 in range [0.0, 1.0])
   - Verifies `hybrid_cells` is None for non-hybrid pages

3. ✅ **Basic output structure validation**
   - Confirms classification is not `Hybrid` or `BrokenVector` for simple vector page
   - Confirms confidence is in valid range [0.0, 1.0]
   - Confirms confidence is reasonable (> 0.5) for clear vector pages

4. ✅ **JSON serialization verification**
   - Tests that `PageClassification` can be serialized to JSON
   - Verifies JSON contains expected fields: `"class":` and `"confidence":`

### Test Parameters

The test uses a simple vector page context:
- `text_op_count: 100` - Has text operators
- `valid_char_count: 490` / `raw_char_count: 500` - 98% character validity
- `image_coverage: 0.0` - No images (pure vector page)
- `density_ratio: 0.90` - High character density
- Standard US Letter dimensions (612 x 792 pt)

## Acceptance Criteria Status

- ✅ **Basic smoke test exists and passes** - Test compiles and runs successfully
- ✅ **Test uses a simple PDF context** - Uses PageContext representing a simple vector PDF page
- ✅ **Output format is verified (JSON structure)** - JSON serialization tested and validated
- ✅ **Function returns successfully for valid context** - No Result type, direct PageClassification returned
- ✅ **Basic output fields are checked**
  - Classification field exists and is reasonable
  - Confidence is in valid range [0.0, 1.0]
  - Confidence is reasonable (> 0.5) for clear vector pages
  - hybrid_cells is None for non-hybrid classification
- ✅ **All tests pass** - Test output: `test classify::tests::test_smoke_classify_basic_vector_page ... ok`
- ✅ **Module compiles without errors** - Clean compilation with only warnings (pre-existing)

## Test Execution

```bash
$ cargo test --package pdftract-core --lib classify::tests::test_smoke
running 1 test
test classify::tests::test_smoke_classify_basic_vector_page ... ok

test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 3189 filtered out; finished in 0.00s
```

## Verification

The smoke test successfully validates:
- The `classify_page` function works correctly with basic input
- Output structure matches expected format
- JSON serialization functions properly
- Basic classification logic produces reasonable results for a simple vector page

## Dependencies

- ✅ Relies on detailed error messages infrastructure (bf-56ilav) - Not needed for this simple test since classify_page returns PageClassification directly, not a Result

## References

- Parent bead: bf-3m5gfj
- Implementation based on existing test patterns in `classify.rs`
- Function signature: `pub fn classify_page(ctx: &PageContext) -> PageClassification`

## Git Changes

**Commit**: `test(bf-1ct908): add smoke test for classify_page function`

**Files Modified**:
- `crates/pdftract-core/src/classify.rs` - Added `test_smoke_classify_basic_vector_page` test function

**Beachhead**: This smoke test provides a basic sanity check for the classify_page function, ensuring it works with valid input and produces properly structured output.
