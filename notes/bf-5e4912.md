# Bead bf-5e4912: Design markdown_structure.pdf fixture content specification

## Summary

Created a comprehensive content specification document for the `markdown-structures.pdf` test fixture.

## Work Completed

### Files Created

1. **`tests/fixtures/markdown/SPECIFICATION.md`** (new file)
   - Authoritative content specification for the markdown-structures.pdf fixture
   - Defines exact text strings for all structural elements
   - Documents Markdown output mapping (expected `extract_markdown()` output)
   - Documents plain text output mapping (expected `extract_text()` output)
   - Provides validation criteria for generated PDFs

### Files Updated

1. **`tests/fixtures/markdown/README.md`**
   - Added reference to SPECIFICATION.md as the authoritative source
   - Updated fixture description to reference the specification

## Specification Content

The SPECIFICATION.md document includes:

### Structural Elements Specified
- **Headings:** 3 levels with exact text content
- **Links:** 3 URLs in different formats (https, email, nested path)
- **Bullet lists:** 5 unordered list items
- **Numbered lists:** 5 ordered list items
- **Tables:** 1 table with 3 columns and 5 rows (including header)

### Exact Text Content
All text strings are specified verbatim:
- Document title: "Markdown Test Document"
- Subtitle: "Testing extract_markdown() vs extract_text()"
- Author: "By Test Author"
- Section headings: "Links Section", "Key Features", "Implementation Steps", "Sample Table"
- Complete paragraph text, list items, and table content

### Output Mappings
- Markdown syntax mapping (e.g., `# Title`, `- item`, `| table |`)
- Plain text mapping (no formatting characters)

### Validation Criteria
- All specified text strings must be present exactly
- Visual hierarchy requirements
- Structural requirements (lists, tables, headings)
- Single-page layout requirement

## Acceptance Criteria Status

✅ **PASS:** Content specification is documented
- Location: `tests/fixtures/markdown/SPECIFICATION.md`
- Format: Markdown document with clear sections
- Accessibility: Referenced from README.md

✅ **PASS:** Specification includes clear structural elements
- Headings (H1, H2, H3) with exact text
- Links with exact URLs
- Bullet and numbered lists with exact items
- Table with exact content and structure
- All elements map to Markdown syntax

✅ **PASS:** Specification is reproducible
- Contains exact text strings for all content
- Includes section-by-section breakdown
- Can be used to verify existing fixture
- Can be used to regenerate fixture
- References the implementation script (`markdown_test_fixture.py`)

## Verification

The specification can be verified by:
1. Reading `SPECIFICATION.md` to understand required content
2. Checking that `markdown-structures.pdf` contains all specified elements
3. Comparing `markdown-structures-expect-markdown.txt` against the Markdown mapping section
4. Comparing `markdown-structures-expect-text.txt` against the plain text mapping section

## Next Steps

This specification serves as the foundation for:
- bf-5e4913: Generate the actual PDF fixture based on this spec
- Verification of `extract_markdown()` behavior
- Verification of `extract_text()` behavior
- Any future regeneration or modification of the fixture

## Commit

Files created/modified in this work:
- `tests/fixtures/markdown/SPECIFICATION.md` (new)
- `tests/fixtures/markdown/README.md` (updated)
- `notes/bf-5e4912.md` (this file)
